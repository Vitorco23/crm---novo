# Projeto Supabase proprio (fora do Lovable Cloud), migrado em 2026-09-03.
# O antigo (mvfglrfcnzfrfgxsaagj) era gerenciado pela organizacao do
# Lovable e ficava inacessivel quando os creditos da conta zeravam.
project_id = "dxcmacdudfrzlgatolzn"

[functions.audit-transcript]
verify_jwt = false

# External providers authenticate inside each handler with their own secret or signature.
[functions.receive-landing-lead]
verify_jwt = false

[functions.receive-matteline-call]
verify_jwt = false

[functions.whatsapp-webhook]
verify_jwt = false

# Callback do OAuth do Google — chamado pelo navegador via redirect do
# Google, nunca por uma sessão autenticada do CRM.
[functions.google-oauth-callback]
verify_jwt = false
