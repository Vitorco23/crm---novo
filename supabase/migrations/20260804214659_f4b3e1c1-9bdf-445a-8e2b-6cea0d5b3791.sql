-- No SQL changes needed as this is a logic refinement in the Edge Function,
-- but the prompt requires updating to enforce the new Stage 1 filtering logic.
SELECT 'Refining priority leads IA logic';
